token = ""  # Your discord bot access token
cache_path = r""  # An empty folder for temporary storage of files
w2x_exec_path = r""  # Path, including executable, to your version of waifu2x
cache_wipe_on_start = True # Will wipe the cache_path on start if True. False is unsupported (for now)
logLevel = "INFO"  # Valid values are DEBUG, INFO, WARN, ERROR, CRITICAL.
